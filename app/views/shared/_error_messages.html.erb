<% if object.errors.any? %>
  <div id= error_explanation>
  	<h2 clas ="alert">
  		<%= pluralize(object.errors.count, "error")%> prohibited from being saved
    </h2>
    <ul>
    	<% object.errors.full_messages.each do |error| %>
    	   <li><%= error %></li>
    	<% end %>
    </ul>
  </div>
<% end %>